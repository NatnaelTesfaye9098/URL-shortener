<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=#, initial-scale=1.0">
    <title>URL Shortener</title>
    <link rel="stylesheet" type="text/css" href="styles/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20,400,0,0&icon_names=content_copy" />
</head>
<body>
    <div class="hero">
        <h1>Make Your Links Short and Simple</h1>
        <P>Long links are a hassle. Shorten them in seconds, share with ease, and track clicks. Try it now!</P>
    </div>
    <div class="input-container">
        <div class="wrapper">
            <form action="/submit" method="POST">
                <input id="textbox" type="text" name="url" placeholder="Enter here" required>
                <input id="button" type="submit" name="submit" value="SHORTEN">
            </form>
        </div>
    </div>
    <%if(locals.response){%>
        <div class="response-container">
            <input type= "text" class="answer" value=<%= response.result_url%> readonly>
            <button type="button" class="copy-link-button" >
                <span class="material-symbols-outlined">content_copy</span>
            </button>
        </div>
    <% }else if (locals.error){ %>
        <p class="error"><%= error%></p>
    <%} %>
    <script>
        document.querySelectorAll(".response-container").forEach(item => {
            const inputfield = item.querySelector(".answer");
            const btn = item.querySelector(".copy-link-button");

            btn.addEventListener("click", ()=>{

                const text = inputfield.value;
                navigator.clipboard.writeText(text);

                inputfield.value = "Copied!";
                setTimeout(()=>{
                    inputfield.value = text;
                }, 1000)
            });
        });
    </script>
</body>
</html>